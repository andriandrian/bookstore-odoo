<odoo>

    <record id="report_transaction" model="ir.action.report">
        <field name="name">Transaction Report</field>
        <field name="model">bookstoe.transaction</field>
        <field name="report-type">qweb-pdf</field>
        <field name="report_name">bookstore.report_transaction</field>
        <field name="report_file">bookstore.report_transaction</field>
        <field name="binding_model_id" ref="model_bookstore_transaction"/>
        <field name="binding_type">report</field>
    </record>

    <record id="report_transaction_id" model="ir.action.report">
        <field name="name">Transaction Report</field>
        <field name="model">bookstoe.transaction</field>
        <field name="report-type">qweb-pdf</field>
        <field name="report_name">bookstore.report_transaction_card</field>
        <field name="report_file">bookstore.report_transaction_card</field>
        <field name="binding_model_id" ref="model_bookstore_transaction"/>
        <field name="binding_type">report</field>
    </record>

    <!-- <template id="report_transaction">
        <t t-call="web.html_container">
            <t t-foreach="docs" t-as="o">
                <center>
                    <h1>Transacntion Report</h1>
                </center>
            </t>
        </t>
    </template> -->

</odoo>