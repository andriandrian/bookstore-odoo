<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <!-- Tree -->
    <record id="view_bookstore_employee_tree" model="ir.ui.view">
        <field name="name">bookstore.employee.tree</field>
        <field name="model">bookstore.employee</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name" string="Employee Name" decoration-bf="1"/>
                <field name="age" optional="show"/>
                <field name="gender"/>
                <field name="position"/>
            </tree> 
        </field>
    </record>

    <!-- Form -->
    <record id="view_bookstore_employee_form" model="ir.ui.view">
        <field name="name">bookstore.employee.form</field>
        <field name="model">bookstore.employee</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <field name="image" widget="image" class="oe_avatar"/>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="date_of_birth"/>
                            <field name="age"/>
                        </group>
                        <group>
                            <field name="gender"/>
                            <field name="position"/>
                            <field name="active" invisible="1"/>
                        </group>
                    </group>
                </sheet>
                <!-- Chatter -->
                <div class="oe_chatter">
                    <field name="message_follower_ids" groups="base.group_user"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form> 
        </field>
    </record>

    <!-- Kanban -->
    <record id="view_bookstore_employee_kanban" model="ir.ui.view">
        <field name="name">bookstore.employee.kanban</field>
        <field name="model">bookstore.employee</field>
        <field name="arch" type="xml">
        <kanban class="o_bookstore_employee_kanban">
            <field name="name"/>
            <field name="age"/>
            <field name="image"/>
            <field name="id"/>
            <field name="image"/>
            <templates>
                <t t-name="kanban-box">
                    <div class="oe_kanban_global_click o_kanban_record_has_iamge_fill d-flex align-items-center">
                        <!-- <div class="o_kanban_image d-none d-md-block"
                            t-attf-style="background-image:url('#{kanban_image('bookstore.employee', 'image', record.id.raw_value)}')">
                            <img class="o_kanban_image_inner_pic d-none d-md-block"
                                t-att-src="kanban_image('bookstore.employee', 'image', record.id.raw_value)" />
                        </div> -->
                        <div class="oe_kanban_image d-none d-md-block">
                            <field name="image" widget="image" options="{'size': [100, 100]}" />
                        </div>
                        <div class="oe_kanban_details d-flex flex-column ml-3">
                                <h2><field name="name"/></h2>
                            <ul>
                                <li style="font-size: 12px">Age : <field name="age"/></li>
                                <li style="font-size: 12px">Position : <field name="position"/></li>
                            </ul>
                        </div>
                    </div>
                </t>
            </templates>


            <!-- <templates>
                <t t-name="kanban-box">
                    <div class="oe_kanban_global_click o_kanban_record_has_image_fill o_bookstore_employee_kanban">
                        <t t-if="record.image.raw_value">
                            <div class="o_kanban_image_fill_left d-none d-md-block" 
                                t-att-style="background-image:url('#{kanban_image('bookstore.employee', 'image', record.id.raw_value)}')"/>
                        </t>
                        <t t-else="">
                            <img class="o_kanban_image" alt="Avatar" t-att-src='_s + "/base/statis/img/avatar.png"'/>
                        </t>
                        <div class="oe_kanban_details">
                            <strong class="o_kanban_record_title"><field name="name"/></strong>
                            <div class="o_kanban_tags_section" />
                            <ul>
                                <li>Age : <field name="age"/></li>
                                <li>Position :<field name="age"/></li>
                                <li>Gender : <field name="age"/></li>
                            </ul>
                        </div>
                    </div>
                </t>
            </templates> -->
        </kanban>
        </field>
    </record>

    <!-- Search -->
    <record id="view_bookstore_employee_search" model="ir.ui.view">
        <field name="name">bookstore.employee.search</field>
        <field name="model">bookstore.employee</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" string="Employee Name" filter_domain="['|', ('name', 'ilike', self), ('ref', 'ilike', self)]"/>
                <field name="age"/>
                <field name="gender"/>
                <filter name="filter_male" string="Male" domain="[('gender', '=', 'male')]"/>
                <filter name="filter_female" string="Female" domain="[('gender', '=', 'female')]"/>
                <separator />
                <group expand="0" string="Group By">
                    <filter string="Gender" name="group_by_gender" context="{'group_by':'gender'}"/>
                </group>
                <separator />
                <filter name="filter_archived" string="Archived" domain="[('active', '=', False)]"/>
                <searchpanel>
                    <field name="gender" select="multi" icon="fa-users" enable_counters="1"/>
                </searchpanel>
            </search> 
        </field>
    </record>

    <!-- Action -->
    <record id="action_bookstore_employee" model="ir.actions.act_window">
        <field name="name">Employees</field>
        <field name="type">ir.actions.act_window</field> 
        <field name="res_model">bookstore.employee</field>
        <field name="context">{}</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first employee.
            </p>
        </field>
    </record>

</odoo>